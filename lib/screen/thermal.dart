import 'package:flutter/material.dart';

class ThermalScreen extends StatelessWidget {
  const ThermalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ThermalScreen",
          style: TextStyle(color: Colors.white),),
      ),
    );
  }
}
